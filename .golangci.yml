run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - revive
    - errcheck
    - ineffassign
    - unused

linters-settings:
  revive:
    rules:
      - name: var-naming
      - name: indent-error-flow

issues:
  exclude-use-default: false